import './globals.css';

export const metadata = {
  title: 'EventPilot — AI event operations',
  description: 'Turn planning conversations into an actionable event control centre.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
